% Calculates and Executes a Constant Curvature Trajectory to the Point at
% (x,y) relative to the robot, with bearing th. [See the note in the
% P2_Robot Class for coordinate standards].
function trajectory_goTo(V, x, y, th)
    l = sqrt(x^2 + y^2); % Distance to Point
    % Calculate Curvature of Path Connecting Two Points:
    k = 2/l)
    % radius of curvature = l/crd(th). K = 1/R
end % #trajectory_goTo